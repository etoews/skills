# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# comparison skill runtime output (generated when the skill runs).
# Anchored with a leading slash so it only matches a top-level output
# directory, never the skills/comparison/ source.
/comparison/

# skill-creator eval / benchmark workspaces
*-workspace/
